---
        - name: "deployer docker"
          user: root
          become: true
          hosts: all
          
        - hosts: registry
          user: root
          roles:
           - registry
            
        - hosts: docker
          user: root
          roles:
                  #- preparation      
              #- deploiement
              #- registry-host



