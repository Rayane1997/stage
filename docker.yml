---
        - name: "deployer docker"
          hosts: localhost
          connection: local
          become: true
          become_user: root
          roles:
           - preparation
           - deploiement




